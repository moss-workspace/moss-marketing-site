---
label: landing
hide_body: true
fields:
- name: header_video
  type: file
  config:
    maxSize: 64
  label: Header video
  description: Remember that the header video crops automatically to the screen size!
pages:
- index.markdown
